server:
  port: 8080

  datasource:
    url: jdbc:postgresql://localhost:5432/payflow_auth
    username: postgres
    password: Shivam@123
    # driver-class-name: org.postgresql.Driver  # Add this

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect  # Add this

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: "my-very-long-and-secure-jwt-secret-key-that-is-at-least-32-characters"
  access-token-expiration: 900000
